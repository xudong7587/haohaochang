import React,{useState} from 'react';
export function SourceImport({request,action,busy,notify}){
 const [url,setUrl]=useState(''),[parsed,setParsed]=useState(null);
 return <div className="settings-card link-import"><h3>解析 MV 链接</h3><p>先查看分 P 标题和识别资料，再下载所选候选。</p><div className="actions"><input aria-label="添加下载链接" disabled={busy} placeholder="粘贴 B站 / YouTube 链接" value={url} onChange={event=>{setUrl(event.target.value);setParsed(null);}}/><button className="primary" disabled={busy||!url} onClick={()=>action(async()=>setParsed(await request('/admin/source-info',{url},'POST')))}>解析 MV 链接</button><button disabled={busy||!parsed?.candidateId} onClick={()=>action(async()=>{await request('/admin/inbox-link',{url,candidateId:parsed.candidateId},'POST');setUrl('');setParsed(null);notify('已将此候选加入下载工作区');})}>下载到待整理</button></div>{parsed&&<div className="link-result"><p>视频标题：{parsed.candidate?.externalTitle||parsed.videoTitle||parsed.title}</p><strong>{parsed.title||'待确认歌名'} — {parsed.artist||'待确认歌手'}</strong><p>{parsed.needs_review?'需要核对歌名、歌手或录音版本，下载后可编辑。':'已识别歌曲信息'}{parsed.duration?` · 时长 ${Math.round(parsed.duration)} 秒`:''}{parsed.candidate?.page?` · 第 ${parsed.candidate.page} P`:''}</p>{parsed.candidate?.canonicalUrl&&<a href={parsed.candidate.canonicalUrl} target="_blank" rel="noreferrer">打开所选视频核对</a>}</div>}</div>;
}
