# 好好唱 · 家庭 KTV · v0.3.10

把想唱的歌留在家里的 NAS，电视或电脑负责播放，手机负责点歌。整理好的画面、原唱、伴奏和歌词可以反复使用，下次打开就能唱。

[下载最新版本](https://github.com/xudong7587/haohaochang/releases/latest) · [详细使用手册](docs/USER-GUIDE.md) · [本版更新](release/RELEASE-NOTES-v0.3.10.md)

## 我需要准备什么？

- **能运行 Docker 的 NAS**：保存歌曲、设置和已点列表，曲库目录需要读写权限。
- **一台播放设备**：Android 电视安装 TV 应用，或在电脑浏览器打开网页歌房。手机扫码即可点歌。
- **麦克风和音响**：连接电视的音响或混音设备。好好唱负责播放音乐，没有手机录音、麦克风返听或唱歌评分。
- **Windows 电脑，可选**：只有普通 MV 时，用它生成伴奏；已有原唱、伴奏双音轨的歌曲可直接整理。也能配置兼容的分离 API 作为后备。

所有设备共用一个歌房、一个已点列表。TV 优先播放；新开的 TV 可以接管旧 TV，网页歌房不会抢走正在播放的 TV。

## 先把第一首歌唱起来

### 1. 在 NAS 安装

从 Release 下载 `haohaochang-nas.zip`，解压后导入其中一份 Compose 配置。

| 使用方式 | 配置文件 |
| --- | --- |
| Linux NAS，希望自动找到同一家庭局域网的 PC | `docker-compose.lan.yaml`，单独使用 host 网络配置 |
| 普通 Docker 部署，或手动填写 PC 地址 | `docker-compose.yaml` |

修改管理密码和三个目录映射：`/data` 保存设置、数据库与任务；`/media` 保存正式曲库；`/download` 暂存下载。管理密码至少 12 位。LAN 配置默认端口为 **43210**，普通配置默认为 **3210**。

镜像是 `ghcr.io/xudong7587/haohaochang:latest`，可以公开拉取。启动后打开 `http://NAS-IP:端口/admin`，输入管理密码。完整安装与迁移步骤见[手册](docs/USER-GUIDE.md#1-安装-nas)。

### 2. 准备歌曲

已有文件就放入映射的曲库目录，在后台扫描；想从网上找，就打开“在线找歌”搜索 B 站视频，试听、选取片段后加入曲库。也可以粘贴链接，或导入 B 站收藏夹。

普通 MV 需要生成伴奏时，下载 `haohaochang-resource-ai.zip`，在 Windows 解压并运行 `start.cmd`。首次会下载运行环境；启动成功后显示整理器窗口，NAS 连接它处理裁剪和分离任务。

曲库分为待整理、半标准、标准三类。**半标准歌曲已有原唱和伴奏，也能唱；歌词缺失不会把已经可唱的歌曲挡在队列外。** 标准歌曲还具备视频画面。需要核对的信息会留在编辑器或任务列表中。

### 3. 打开播放设备，手机扫码

电视从 Release 下载安装 `haohaochang-tv.apk`（不通过应用商店分发），输入 NAS 根地址，按电视提示用手机扫码登录。没有电视时，在后台打开“网页歌房” `/play`。

手机扫描歌房二维码，按歌名或歌手点歌，也能暂停、切歌、切换原唱／伴奏和发送掌声。无人点歌时随机播放原唱，已点歌曲就绪后接管播放。

## 平时怎么用？

- **手机没搜到歌**：进入“在线找歌”，优先选 B 站视频；没有合适画面可点“先找音频 + 歌词”。手机任务优先于后台找歌排队，完成后自动加入已点列表。画面下载失败会尝试音频方案；找不到可靠歌词或资源时仍需要核对。
- **管理曲库**：默认直接展开歌曲，可以切换行列表和海报墙。封面优先使用歌曲或专辑图片，没有时尝试歌手照片。筛选、分页、批量整理和编辑在两种视图中一致。
- **修改一首歌**：点击“编辑歌曲”，在同一窗口调整资料、视频、歌词和维护操作。结果与错误会在窗口内显示，未保存的草稿会保留。
- **MV 已有字幕**：在网页歌房或 TV 点“隐藏歌词”，关掉系统滚动歌词和时间微调控件；视频自身的字幕保留。选择会记在当前浏览器或 TV 内。
- **查看 PC 工作情况**：整理器窗口、右下角托盘和 NAS 的 `/pc` 页面都能查看任务。窗口显示 GPU、显存、CPU 和内存占用；关闭窗口继续后台运行，通过“退出整理器”结束空闲服务。

补视频不会默默替换已经可用的原唱和伴奏。视频默认保留原编码与分辨率；只有播放设备不支持时，才需要手动转换兼容画面。

## 怎么更新？

| 部分 | 更新方法 |
| --- | --- |
| NAS | 手动拉取新 Docker 镜像并重建容器，保留密码、端口和目录映射 |
| TV | 从本版起，遥控器菜单键 → 检查应用更新 → 下载 → 系统确认安装 |
| PC 整理器 | 窗口、托盘、详细页面或 NAS `/pc` → 检查更新 → 安装新版 |

**首次升级到 v0.3.10，TV 和 PC 仍要手动安装一次。** TV 本版开始使用固定发布签名，旧 debug 测试包可能无法覆盖，需卸载后安装并重新连接 NAS、扫码登录。PC 手动覆盖前先退出旧进程，保留 `worker.json`、`runtime`、`.venv` 和 `data`。

之后 PC 应用内更新会校验下载包、等待当前任务完成、备份程序并重启；新版启动失败时恢复旧程序。配置、运行环境、模型与任务数据保留。更新依赖设备能访问 GitHub。

## 先了解这些边界

外部下载源和歌词库可能暂时不可用；“没找到”不一定是歌名填错。手动查找 LRC 会接受时长不一致的候选并提示核对，自动匹配仍检查歌名、演唱者和时长。AI 分离效果随录音而变，不能保证每首歌的人声都完全消失。

本版包含本地自动化验证；实际电视安装、遥控器、解码能力以及家庭网络连接仍需在自己的设备上确认。公司环境的开发测试只使用 localhost，未自动部署到真实 NAS。

遇到问题先看[排错与常见问题](docs/USER-GUIDE.md#10-常见问题)，再查看 NAS 的任务详情或导出诊断日志。

开发者入口：[开发说明](docs/DEVELOPMENT.md) · [当前实现](docs/PROJECT-STATUS.md) · [验证记录](docs/VALIDATION.md) · [模块与接口](docs/DEVELOPMENT-PLAN.md)。每次发布会同步 README 的功能说明与版本号。
