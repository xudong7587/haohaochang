这次修复了选择最高画质仍下载480p的问题，并加入TV手机扫码联动登录和返回退出。

- B站登录已过期时明确提示；管理端可直接扫码，在线下载与收藏夹共用账号。下载改用WBI签名的B站DASH接口，检查账号权限与实际分辨率，最高至少720p，不能静默回退低清。
- 视频与音频独立下载、按同一区间裁剪，音频直接用于人声分离。已有在线歌曲可“升级高清画面”，保留现有原唱、伴奏、歌词与旧版本。
- 没有歌词时不显示待补充提示；新版PC返回人声起点，仅在多句LRC支持同一偏移时自动校准，并可恢复原稿。
- 批量重新识别歌名与歌手改为每批20首，避免逐首请求引发429；刷新列表说明清晰，移除扩充目录界面。
- TV连接NAS后直接显示二维码，手机验证密码并确认后电视自动登录，手机继续点歌。管理密码不保存在电视；二维码三分钟有效。
- APK返回键逐层退出弹层/全屏、回到主界面，再连续按两次返回关闭应用。兼容旧系统和Android13以上返回回调。
- 关闭或拖动在线预览造成的请求取消不再刷Premature close错误，真正上游错误仍保留。

验证：本地107项Node测试、15项Python协议测试、前端构建及5组浏览器检查通过；GitHub Actions另含全部浏览器检查、Python并发、PC发现、Windows启动器、APK构建和容器检查。实际B站视频BV1xg1MBmEQe在重新扫码后已完整下载1920×1080 H.264，并按原裁剪区间生成290.320秒的1080p画面；4K仅做合成媒体保留分辨率验证。自动歌词校准采用能量起点检测，不能替代歌词内容识别；真实歌曲校准效果和电视遥控器仍需实机验收。

升级：请同时更新NAS镜像、PC整理器和TV APK，保留现有配置、数据库和媒体目录。NAS升级后在管理端“在线找歌”重新扫码登录B站，再对已有低清歌曲使用“升级高清画面”。此次发布不会直接修改你的NAS曲库。

附件：NAS部署包、PC整理器、重命名工具、新版TV APK及SHA256校验文件。
