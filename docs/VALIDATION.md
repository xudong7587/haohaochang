# v0.3.13 本地验证（2026-09-10）

## v0.3.13 连续播放与全屏遥控菜单

- 曲库新增跨所有分页的全选勾选，范围为当前分类及搜索结果；新增整个未隐藏曲库／所选歌曲的缺失歌词补充任务，保留已有歌词与音轨，播放队列内或整理中的歌曲跳过。后台持久任务支持失败原因与重试。147 项测试包含认证、全库范围、去重、修订冲突、查找失败、入队竞争与音轨字节保留；浏览器覆盖 45 首跨页全选、分批提交与全库入口。

- 根据用户实机反馈：TV 卡顿而平板 /play 流畅。确认 NAS 使用 Express sendFile 的 HTTP Range 文件直读，播放路由没有实时转码。原控制器每 100ms 检查音画差，超过 120ms 直接设置 currentTime；按用户要求删除整个周期校时定时器，开始／恢复、显式 seek、切换音轨与错误后备才做对齐。歌词只读取时钟。
- 歌词时钟移入独立 PlayerVisuals，避免约 30 次／秒更新整个 Player；显示层降到最多 20 次／秒，视频与音频原速解码。
- 全屏下键／确认键呼出菜单并聚焦暂停，方向键可移动到切歌和歌词按钮，首次唤醒及长按不触发操作。APK 保留在原沉浸式 WebView 内全屏，避免系统 custom video view 遮住网页控制层。
- 本地 147 项 Node 测试、生产构建与播放器浏览器回归通过。隔离测试覆盖无周期调度、视频人为慢速时没有自动 seek、错误后备时钟、V2 文件 HTTP 206，以及全屏下键／确认唤醒、暂停／继续、歌词调整和切歌。真实双音频、接管、自动播放拦截、歌词与旧资源回归通过；雷鸟 TV 实际流畅度未在本机验收，生产 NAS 未改动。

---

# v0.3.11 本地验证：封面编辑与中文歌词来源（2026-09-10）

## v0.3.12 电视连接、歌星资料与移动播放

- APK 首次自动发现 NAS，连接后显示重新设计的扫码页；地址页支持遥控器焦点、手动输入和重试。NAS 增加 UDP 43212 发现响应与公开服务身份校验，LAN 配置启用，发现不返回任何登录凭证。
- 已复现并修复登录后 NodeList 数组调用 Array.at 导致旧 WebView 白屏的问题。生产构建提供 Chrome 53 目标兼容包与必要 API 补丁，加载失败保留重试界面；Docker 纳入启动脚本和图标。兼容目标不代表全部 Android 系统或解码器已经验收。
- 歌曲封面在 NAS 居中裁方并铺白底，避开 FFmpeg 5.1 packed RGBA drawbox replace 路径。无效图片或保存失败不修改旧封面与音轨。
- 歌星资料独立存储照片与介绍，后台可编辑，支持 B站候选／上传／歌手图查找；缺失照片低优先级尝试补齐，已有资料保留。可信短介绍来自同名唯一 Wikidata 演唱者条目，无结果保留空白或手写文字。管理歌曲墙点击编辑，play／TV 深色歌曲墙点击点歌，队列与分页也统一深色。
- TV 舞台及全屏的歌词工具栏播放时闲置 4 秒隐藏，键盘／遥控器／触摸可唤醒，暂停与错误时可见。APK 在手机使用本机网页播放器，横竖屏切换保持 WebView，TV 保留横屏与播放优先权。
- 本地 143 项 Node 测试、前端构建、电视配对／封面编辑／新增交互浏览器验证通过。Android assembleDebug 与 lint 通过；5 项 Java 策略测试用直接 JUnit 运行通过（本机中文路径下 Gradle 测试类加载失败，Ubuntu CI 运行完整任务）。真实雷鸟鹏7 FF85S575C-4FAA 的安装、解码、遥控器与 LAN 广播尚未实测；未部署 NAS、未探测公司局域网。正式发布结果见 Actions 与 Release。


- `npm test` 137 / 137 通过；构建、曲库、已有海报与新增封面编辑浏览器检查通过。透明 PNG 转 JPEG 的白底显示已视觉核对，并重新执行封面转换与编辑回归。
- 封面测试包含管理员认证、候选有效期、旧修订拒绝、已有封面替换、原始图片上传、无效图片保留旧图、取消选择、只读预览禁写、移动端布局与音频不变。B站搜索只生成图片候选，不创建下载任务。
- 中文歌词测试覆盖 QQ 与网易云响应解析、时间戳、文字实体、来源后备、无时间戳拒绝、歌手／录音版本候选及超大响应／重定向拒绝。浏览器验证多候选不会覆盖草稿，用户选择后才载入，保存请求不携带整份候选列表。
- 本地真实预览已通过：周深《沉默的羔羊》B站搜索返回 20 张候选；QQ 音乐返回周深 Live 版本的 73 行带时间戳歌词和其他版本候选。网易云在当前公司网络的 TLS 连接被重置，真实连接未通过验收；协议替身和失败后备通过。
- 本轮在 `codex/cover-picker-lyrics-candidates` 开发，用户授权推送、合并 main 和发布。生产 NAS 未写入；发布结果以 [v0.3.11 Release](https://github.com/xudong7587/haohaochang/releases/tag/v0.3.11) 与同提交 Actions 为准。预览仍是 `http://127.0.0.1:3212/admin`。

---
# v0.3.10 本地验证（2026-09-10）

- `npm test`：130 / 130 通过，含真实 FFmpeg；`npm run build`、版本一致性检查通过。
- Python：协议 15 项（显式设置本地 FFmpeg／ffprobe 后无跳过）、并发 1 项、LAN 2 项、更新器 8 项通过。更新器覆盖恶意路径／文件清单／哈希、忙碌等待与取消、拒绝新任务、部分替换回滚、真实 localhost 子进程重启以及新版启动失败恢复旧服务。配置与模型文件保留检查通过。
- 曲库、海报、在线播放器、NAS PC 页面、PC 本地网页浏览器检查通过，覆盖默认展开、两种视图、390px 无溢出、弹窗内反馈、手机在线整理与音频后备请求、歌词隐藏与全屏、更新代理认证／字段筛选和更新按钮。
- 最后回归 `scripts/ui-check.mjs`、`scripts/player-check.mjs`、TV 扫码、反馈和播放接管浏览器检查全部通过；修正旧 UI 检查依赖默认歌手折叠的选择器，改为断言默认不分组。
- Windows 重命名工具与隐藏启动链检查通过；原生整理器窗口在独立 localhost 状态服务下创建并响应。未完成原生按钮逐项视觉验收，也未运行真实家庭 GPU 模型任务。
- 真实只读曲库预览：241 首均有图片，此前缺图 79 首已补齐；《裹着心的光》手动 LRC 查询从 LRCLIB 获得 41 行。正式 NAS 数据库与媒体未修改，预览只允许读取歌词草稿。
- Android 单元测试、debug／正式签名 APK 构建和 Docker 镜像容器检查由 GitHub Actions 执行，结果以同提交工作流为准。TV 系统安装／覆盖更新、遥控器、家庭 PC 完整更新与 NAS 部署需实机验收；断电／磁盘故障恢复不在当前自动回滚保证内。

下方保留各阶段历史记录，当前版本范围以上述记录和 Release 为准。

---
# 后续本地验证：深色卡片与歌手图片（2026-09-10）

- 122 项 Node 测试与构建通过。新增 `tests/poster-fallback.test.js` 验证专辑优先、歌手身份筛选、方形照片、随机同歌手专辑图片、合唱后备和旧策略失败重试；已有资源保护测试继续通过。
- `tests/poster.browser.mjs` 新增 play／TV 深色卡片背景及歌名、歌手、状态文字至少 4.5:1 的实际浏览器对比度检查，原唱预览、手机双列与封面入口回归通过。
- 周深真实歌手页面与照片下载成功；本地预览 79 首缺图已全部补齐，周深搜索的 10 张图均加载成功。截图 `test-results/artist-fallback/play-zhou-shen.png`。只写本地预览缓存，尚未发布。

# v0.3.9 本地验收：工作台、封面与播放接管（2026-09-10）

- 分支 `codex/task-workbench-redesign`，以 v0.3.8 为基线；发布记录见 Release 和 Actions。
- `npm test`：117 / 117 通过（包含真实 FFmpeg）；`npm run build` 通过。
- `scripts/ui-check.mjs`、`scripts/player-check.mjs`、`tests/library-ui.browser.mjs`、`tests/feedback.browser.mjs`、`tests/pc-dashboard.browser.mjs`、`tests/online-player.browser.mjs`、`tests/tv-pairing.browser.mjs` 均通过。
- `tests/player-lease.test.js` 与 `tests/player-lease.browser.mjs` 覆盖新页面接管、TV 优先、独立标识、被接管后音轨暂停、旧结束事件拒绝、离线回退、成功响应延迟；已纳入 CI。
- 自动封面补齐验证批量上限、在途歌曲和隐藏歌曲跳过、24 小时重试间隔。
- `tests/poster.test.js` 覆盖正确歌手／繁简匹配、B站封面优先、重定向边界、图像格式校验、真实 JPEG 转换、失败保留旧封面与可唱状态、预览禁写与禁后台调度。
- `tests/poster.browser.mjs` 覆盖歌手墙 24 首分页、原唱独立播放、视频时钟、暂停／关闭释放音轨、不改变队列、目录入口、play／TV 图片、390px 手机双列无溢出、带正确修订号的封面任务和无需登录读取图标。
- `tests/pc-worker-ui.browser.mjs` 使用隔离 localhost 状态，156 项任务初始仅显示 1 项运行、每页 10 条排队记录、翻页与手机宽度通过。
- 真实 NAS 媒体使用 junction 和独立 241 首数据库进行只读预览；本机 Chrome 的 3840×2160 HEVC 歌曲视频与原唱均能播放。自动填充封面写入本地 `data/local-nas-preview/posters`。未写入生产 NAS 或曲库样例。
- Windows 映射盘 native realpath 返回 UNKNOWN 时使用跟随链接的兼容解析，仍检查真实目录边界；服务关闭等待自动封面任务释放数据库。修复后的全套 Node 测试通过。
- 真实手机、家庭电视、家庭 PC 新界面与 NAS 部署仍待用户确认后验收。

---

# 验证记录

2026-09-07，Windows 开发机，Node.js 24.18.0。

已完成：

- `npm run build`：生产前端构建成功。
- `npm test`：9 组测试通过，覆盖认证、管理员隔离、跨站写入、队列并发去重、过期切歌保护、拼音搜索、纯音频标记、单电视租约、媒体 Range 请求、AI 密钥不返回前端及 URL 约束。
- 真实 FFmpeg：合成音频封装 H.264/AAC、双音轨输出为不同文件、缓存时间戳保持不变、模拟 AI 上传与真实结果封装通过。
- `scripts/ui-check.mjs`：Edge 无头浏览器验证电视方向键、搜索空状态、二维码、手机点歌到电视、鼓掌上屏、后台设置和编辑，共 8 项；没有 JavaScript 页面异常。
- Android Gradle `assembleDebug`：构建成功。测试 APK 位于 `release/haohaochang-tv-0.1.0-debug.apk`，已使用 Android 构建工具进行签名验证。
- `scripts/preview-check.mjs`：本机预览台验证通过，包含虚拟遥控器、真实视频播放、原唱切换保留播放时间、音频同步歌词，以及后台/手机预览切换。

尚未验证：

- UGOS Pro 实际目录权限与设备性能。镜像已在 GitHub Linux Runner 完成构建和容器启动检查，开发机没有 Docker。
- 雷鸟电视安装、遥控器具体键值、电视 WebView 版本、系统解码器及音响链路。
- NAS 网络下的 Bilibili/YouTube 在线搜索、下载成功率与速度；没有以下载第三方歌曲作为测试样本。
- 真实 AI 推理音质和 CPU/GPU 吞吐。Demucs 容器已提供，但本机未拉取模型或执行分离推理。
- 大曲库、长时间连续播放、磁盘满载、休眠唤醒等压力场景。

现场验收顺序：先用一首确认无误的本地双音轨视频，验证伴奏默认、原唱切换和切歌；再用手机点第二首及发送掌声；接着测试 MP3 + LRC 回退；最后启用外网下载和 AI。这样可以分别定位媒体、网络、模型的问题。

两次现场点播同一首已分离歌曲时，后台应不再创建分离任务。出现停顿时先查看媒体是否处于预处理阶段，再检查任务错误和电视系统媒体能力。

2026-09-07 补充：GitHub Linux CI 与镜像启动检查已通过；公开镜像匿名读取已验证。设置已迁移到自动生成的 settings.json，新增初始化、旧配置迁移、重启读取及配置损坏保护测试。

2026-09-07 反代修复：新增根路径跳转、HTTPS 外部 Origin、非标准端口二维码、反代改写 Host 后的配置地址校验、恶意转发头拒绝及 SSE 首帧测试。共 10 组测试通过，Web 构建和浏览器联动通过。用户已确认 UGOS 容器启动；实际 Lucky、电视异地播放仍待用户验收。

自动曲库与 TV 舞台更新：新增 NFO/海报读取、标签筛选、重复导入保留源文件、随机原唱与点播接管测试；真实 FFmpeg + 模拟远端 AI 验证完整入库双版本流程。stage-check 验证 720p 自动原唱播放、点歌切伴奏及整理预览。bili-sync 已核对源码格式，NAS 实际自动同步与真实 AI 音质仍待现场验证。

## 2026-09-08 本轮交付

- 完整测试 16 组通过，覆盖收藏夹分页（模拟请求）、结构化信息识别与来源核对（模拟 API）、待核对权限、原文件保留、标签和手工资料保护。
- 真实 FFmpeg 配合模拟分离服务验证 PC 优先、PC/API 并行、PC 离线回退及导入双版本；未执行真实 GPU 模型或收费 API。
- 前端生产构建、浏览器联动和 stage-check 通过；720p 默认随机原唱、点播伴奏接管及整理预览已检查。
- PC Python 语法与 PowerShell 启动脚本语法检查通过；首次依赖安装与 4070 Super 推理待实机验证。
- 功能提交 826aa07 的 GitHub 检查通过，[镜像发布任务](https://github.com/xudong7587/haohaochang/actions/runs/34176288228) 的主服务和分离服务构建、启动检查均成功。
- 本轮交付四端使用说明；旧记录中的 Origin 限制已移除，目前使用密码/客厅凭证验证身份。旧测试数量为对应阶段记录，以本节为准。

网页歌房补充：/play 独立入口、后台新标签打开并复用客厅凭证、模拟反代地址的二维码免密加入和手机掌声上屏通过；16 组回归与生产构建通过。浏览器反代测试使用请求转发模拟，手机 SSE 未在该模拟中转发，真实 Lucky 长连接仍需现场验收。

收尾验收：网页歌房全屏二维码展开/收起、第二播放设备显示占用提示且保持暂停通过；UI 联动复测无页面异常。本机预览已重启。

按最终交互调整：全屏二维码改为角落常驻，队列预告定时显示；已验证有队列时出现、6 秒后收起而二维码继续可见，生产构建通过。

## 2026-09-08 资源 AI 整理器与正式资源更新

- Windows RTX 5080 Laptop GPU：PyTorch 2.7.1/cu128，真实 CUDA 运算检查成功。
- 45 秒合成单声道音频经解码、Demucs htdemucs、NAS 结果回传与双版本封装成功，任务约 21 秒。该样本用于链路/兼容验证，不代表真实音乐分离音质。
- 修复首次环境安装版本兼容、GPU 分段参数及单声道张量问题；模型按官方哈希验证。
- PC 仅监听 127.0.0.1:8000，本地 NAS 模拟仅监听 127.0.0.1:3210；未修改公司局域网或防火墙。
- 资源 AI 整理器状态页渲染通过，无页面异常，显示实际任务、CPU/内存/GPU 数据。硬件选择覆盖新 NVIDIA、旧驱动、低显存、无 GPU 的单元场景。
- 18 组测试通过；覆盖正式资源迁移保留旧文件、历史版本、自动候选排除错误匹配、未找到转人工处理。
- 真实 FFmpeg 频谱及多图片输入渲染通过，Web 构建和 UI/stage 联动通过。
- 自动平台下载与收费 AI 没有使用第三方真实歌曲/密钥做验收；MTV 候选需管理员确认版本与同步，未自动覆盖。


## v0.2.0 第 0／1 批 · 2026-09-09

当前验收取代上文旧测试数量：Windows 本地 Node 测试 69 项全部通过，包含真实 FFmpeg；生产前端构建通过。`scripts/ui-check.mjs` 的 11 项工作台／TV／手机联动通过，`tests/library-ui.browser.mjs` 的编辑草稿、候选、来源、核对动作与隐藏恢复通过，`scripts/player-check.mjs` 的双路失败、原唱切换、暂停、租约、画面提前结束、旧格式时间轴、原生声音授权失败与点击恢复通过。Edge 无头运行无页面脚本错误，截图存于 test-results。

CI 配置同步执行 Node、三类浏览器测试、Python 分离协议 9 项测试、Docker 主服务构建以及 Android debug APK 构建。镜像发布按提交 SHA 做启动检查，两套镜像都成功后才更新 latest；最终运行结果随发布记录补充。

本批全程使用隔离数据库、合成媒体与 localhost；未向实际曲库添加示例。真实 NAS／手机／TV 音响、长期播放、音乐 GPU 分离音质和收费 API 尚未重新验收。此前合成 CUDA 样本结果仅为历史记录。


发布前 CI：提交 `b59256fad9392bfeff4fc24209e49008e6679b3d` 的 [Validate and build](https://github.com/xudong7587/haohaochang/actions/runs/34250451823) 全部成功：69 项 Node 回归、三类 Chromium 浏览器验收、9 项 Python 协议测试、Docker 主服务构建和 Android debug APK 构建。两次旧失败记录来自播放器测试读取状态时意外授予用户激活；改为不授予激活的 CDP 状态读取后，本地 Chromium／Edge 和 Linux CI 均通过，原生声音授权断言保留。


v0.2.0 发布提交为 `f50c823395981343fcff69bbd8740169b84dc408`，已合并 main，并将 main 设为默认分支，master 保留。该标签的 [完整 CI](https://github.com/xudong7587/haohaochang/actions/runs/34250820363) 和 [两套镜像构建／启动／latest 提升](https://github.com/xudong7587/haohaochang/actions/runs/34250820961) 均成功。APK 从该标签 CI 新构建，清单版本 0.2.0，ZIP 完整性与 SHA256 已检查；未重新使用旧 APK。

## v0.3.0 · 2026-09-09

本地 Node 76 项通过（含真实 FFmpeg），Python 分离/裁剪协议 11 项、模拟 LAN 协议 2 项通过。四类浏览器检查覆盖原有工作台、草稿与播放器异常，以及新视频瀑布流、纯流 A/V 播放/暂停/定位、播放和暂停时的区间标记、非法区间、默认整段、搜索身份保留、390px 布局、歌词倒数与遥控器左右键；测试资源全部位于临时目录和随机 localhost 端口。构建通过。

真实公开 B站搜索返回 20 条；视频预览解析得到独立画面/音频流，NAS 代理两路 Range=bytes=0-1023 均返回 206 与 1024 字节。yt-dlp 重复请求曾触发平台 412，改为播放器 API 优先、yt-dlp 后备，并增加缓存与重试提示。此验证不代表所有视频、Cookie、地区或未来平台变化均可用。

PC 裁剪使用真实 FFmpeg，4 秒合成视频取 1.25–2.75 秒，结果时长在 0.15 秒容差内且保留音视频轨道；原输入保留，重试复用任务/结果。LAN 发现测试通过内存 UDP 替身及 localhost HTTP 验证，不代表已测试家庭广播、Docker host 网络或 Windows 防火墙。没有做本版本真实音乐 GPU 音质或家庭 TV 音响验收。发布提交 f9739bdfaa1edc73474a6436d9b17068c2a17316 的 [标签 CI](https://github.com/xudong7587/haohaochang/actions/runs/34312286038)、[main CI](https://github.com/xudong7587/haohaochang/actions/runs/34312285811) 和 [两套镜像启动检查及 latest 更新](https://github.com/xudong7587/haohaochang/actions/runs/34312286052) 均全部成功。

## 2026-09-09 · v0.3.1 本地验证

- `npm test`：77 项通过，包含真实 FFmpeg 媒体处理。新增 pc-flow.test.js 验证 4 秒视频标记 1.25–2.75 秒后，分离接口收到约 1.5 秒音频；不标记收到全长；裁剪失败不派发导入，原视频字节不变。模型输出使用协议替身，此项不证明去人声音质。
- `npm run build` 通过；ui-check、player-check、library-ui、online-player、pc-dashboard 五类浏览器检查通过。新增页面覆盖管理密码、只访问 NAS 同源、PC 密钥及私有路径过滤、在线/断线、刷新与会话、390px 手机布局、管理端“歌星管理”。截图保存在 test-results/pc-dashboard。
- Python 工作协议 11 项、模拟 LAN 2 项通过。Windows launcher-check 真实执行 start.cmd→open.vbs→隐藏 PowerShell，验证关闭自动浏览器和保留 local-only 设置；安装入口使用临时替身，未下载模型、未启用 LAN。
- 家庭 NAS 反代配置、广播发现与电视实机未在本轮验证；发布检查以 v0.3.1 标签的 Actions 和 Release 为准。
# v0.3.2 曲库管理回归 · 2026-09-09

- 本地 `npm test` 80 项通过；真实 FFmpeg 验证无歌词仍完成导入和音轨分离，旧裁剪顺序与资源保留回归继续通过。
- 独立数据库和临时媒体目录验证永久删除：展示目标目录、阻止任务/队列占用、拒绝过期确认、保留无关文件；下载区 105 文件（104 已排队）全部可见，未处理文件可以确认删除。
- 五类浏览器检查通过：整体管理/点歌、曲库草稿与删除确认、在线选段、PC 管理与日志下载、播放器异常恢复。PC/备用 AI 独立保存和检测由 localhost 协议替身验证。
- 未操作用户正式曲库；未探测公司局域网或修改防火墙。本地通过不代表家庭 NAS、GPU 音质或电视实机通过。发布构建结果见版本 Release 与 Actions。

## 2026-09-09 用户反馈优化 · 本地验收

基线为 GitHub `main` 的 `c285a2e`，保留此前未跟踪的预处理工具并纳入版本管理。本轮没有推送 GitHub、更新 NAS 镜像或发布 Release。

- `npm test`：85 项全部通过，包含真实 FFmpeg；新增默认在线迁移和主动关闭保留、三路后台与在线预留槽、在线子任务优先级、超过 50 项的在途任务展示与进度、200 项独立在线容量、半标准批量整理。
- `npm run build` 通过。`scripts/ui-check.mjs`、`tests/library-ui.browser.mjs`、`tests/feedback.browser.mjs`、`tests/online-player.browser.mjs` 和 `tests/pc-dashboard.browser.mjs` 通过。覆盖设置分区、真实步骤百分比、50 条任务、移动端宽度、切换分类和设置保留草稿、歌手折叠后的删除与恢复。截图位于本地 `test-results/feedback/`。
- Python 协议测试 11 项通过（设置 FFMPEG 与 FFPROBE，包含实际裁剪）；`separator/test_concurrency.py` 额外 1 项通过，验证三个请求并发执行且第四个排队，进度通过协议返回。Windows 隐藏启动测试通过，使用隔离替身、无 LAN。
- 真实本机 RTX 5080 Laptop GPU / CUDA / htdemucs 同时处理三份 12 秒合成音频，峰值三项并发，约 11 秒全部完成并校验输出。数据与任务均在独立临时目录，复用已有模型文件，没有向实际曲库添加示例。结果记录于 `test-results/feedback/gpu.json`。这是实际 GPU 并发验证，不等同真实歌曲音质或长期显存压力验收。
- 预处理核心与 WinForms 渲染通过：普通文字替换、批量删除《》、字面量匹配、重复目标拒绝、媒体内容不变、NFO 备份和锁定文件回滚。`Z:\V3 Media\Haohaochang\预处理\RenameTool` 已更新，旧程序备份在其 `code-backup-20260909-164222` 中，未执行用户媒体的批量重命名。
- 已安装的 `C:\Users\Sunny\AppData\Local\Haohaochang\PCWorker` 程序同步，保留 worker.json、runtime、模型和数据。新版分发包为本地 `release/haohaochang-resource-ai-local.zip`。

运行服务边界：自动审批拒绝“停止现有 3210 预览进程”和“启动新的 3211 验收服务”，均只返回 `blocked by policy`。这些启动／停止命令未执行；原 3210 后端仍是旧进程。需手动重启本地服务后再进行完整交互验收；已有 1 首实际歌曲未改变。NAS、家庭 PC、电视和音响仍待用户验收。

## 2026-09-09 · v0.3.5

基线 GitHub main `55e2f89`。所有本地测试使用隔离数据库、临时合成媒体与 localhost，未访问用户 NAS 或更改其曲库。

- Node 97 项通过：新增同 IP 后台读取和媒体请求洪峰后歌词／暂停／心跳仍成功，写入限流与 Retry-After 保留；任务事件合并与状态即时发送；B站会员格式参数、实际档位、预览／下载缓存隔离；真实 1440p NAS 转换及转换／校验百分比。既有剪辑、歌词、资源版本、失败保留、队列、恢复和下载兼容回归通过。
- Python 17 项通过（协议 14、并发 1、LAN 替身 2）。PC 实际 FFmpeg 输出验证保持 2560×1440、H.264 无声画面与原始文件，NVENC 失败到 CPU 的测试使用替身。本轮未执行真实 GPU 或 LAN 扫描。
- 前端构建及浏览器检查：scripts/ui-check.mjs、tests/library-ui.browser.mjs、tests/online-player.browser.mjs、tests/feedback.browser.mjs、tests/pc-dashboard.browser.mjs。新增 TV 播放中右键从导航进入功能区且不改歌词、从画面向下到全屏按钮并确认、全屏内焦点、返回退出、原生 API 被拒时铺满 viewport；清晰度切换保留时间／裁剪标记，六个歌词按钮请求量和值正确。
- scripts/player-check.mjs 通过：真实双音频、暂停／切换、媒体失效回退、租约、切歌清理、原生全屏、自动播放被拒后的恢复。GitHub CI、APK 和镜像发布结果在本节后续补充，正式状态见 Release 和 Actions。用户购买大会员后的可用档位、真实下载与 TV 物理遥控器未在本机替代验收。

- 首次 Linux CI 在原生全屏退出／后备全屏立即进入时发现迟到事件覆盖状态，已区分原生／后备全屏模式，并添加主动派发迟到 fullscreenchange 的回归，修复后重新验证。

## v0.3.7 本地验证（2026-09-10）

111 项 Node 测试与 16 项 Python 协议测试通过。新增清理测试确认：受管歌曲多份画面收敛到一份；既有播放画面、原唱、伴奏、歌词逐字节不变；旧来源的全部音轨无损保留，后续重新准备仍是可点唱标准资源。排队、失效资源、变化或被引用的下载输入受到保护。4K AV1 与真实 HEVC 原画面封装前后码流 SHA-256 相同；20.02 秒 AV1 用时 1.4 秒、238.267 秒 HEVC 用时 8.6 秒，仅为开发机画面阶段。用户所给 B站链接使用当前账号在 localhost 代理后实际播放 1080p 视频和音频成功；未原样复现其 NAS 间歇失败。

生产 NAS 保持用户停机状态，未对其媒体、数据库或配置执行写入。实际 NAS 吞吐量和电视对 HEVC/AV1 的支持需部署后验收。

## v0.3.8 歌词控制（2026-09-10）

111 项 Node 测试通过；两分钟差值边界接受，超出拒绝，错误歌名／歌手仍拒绝，录音版本标签不同与缺少时长不再单独拒绝。浏览器覆盖隐藏歌词后无字幕／倒计时／微调栏、刷新记忆、全屏恢复显示，以及切换时音视频元素与音轨实例不变、播放时钟继续推进。生产 NAS 未改动。
