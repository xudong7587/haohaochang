# 验证记录

2026-09-07，Windows 开发机，Node.js 24.18.0。

已完成：

- `npm run build`：生产前端构建成功。
- `npm test`：9 组测试通过，覆盖认证、管理员隔离、跨站写入、队列并发去重、过期切歌保护、拼音搜索、纯音频标记、单电视租约、媒体 Range 请求、AI 密钥不返回前端及 URL 约束。
- 真实 FFmpeg：合成音频封装 H.264/AAC、双音轨输出为不同文件、缓存时间戳保持不变、模拟 AI 上传与真实结果封装通过。
- `scripts/ui-check.mjs`：Edge 无头浏览器验证电视方向键、搜索空状态、二维码、手机点歌到电视、鼓掌上屏、后台设置和编辑，共 8 项；没有 JavaScript 页面异常。
- Android Gradle `assembleDebug`：构建成功。测试 APK 位于 `release/haohaochang-tv-0.1.0-debug.apk`，已使用 Android 构建工具进行签名验证。
- `scripts/preview-check.mjs`：本机预览台验证通过，包含虚拟遥控器、真实视频播放、原唱切换保留播放时间、音频同步歌词，以及后台/手机预览切换。

尚未验证：

- UGOS Pro 实际目录权限与设备性能。镜像已在 GitHub Linux Runner 完成构建和容器启动检查，开发机没有 Docker。
- 雷鸟电视安装、遥控器具体键值、电视 WebView 版本、系统解码器及音响链路。
- NAS 网络下的 Bilibili/YouTube 在线搜索、下载成功率与速度；没有以下载第三方歌曲作为测试样本。
- 真实 AI 推理音质和 CPU/GPU 吞吐。Demucs 容器已提供，但本机未拉取模型或执行分离推理。
- 大曲库、长时间连续播放、磁盘满载、休眠唤醒等压力场景。

现场验收顺序：先用一首确认无误的本地双音轨视频，验证伴奏默认、原唱切换和切歌；再用手机点第二首及发送掌声；接着测试 MP3 + LRC 回退；最后启用外网下载和 AI。这样可以分别定位媒体、网络、模型的问题。

两次现场点播同一首已分离歌曲时，后台应不再创建分离任务。出现停顿时先查看媒体是否处于预处理阶段，再检查任务错误和电视系统媒体能力。

2026-09-07 补充：GitHub Linux CI 与镜像启动检查已通过；公开镜像匿名读取已验证。设置已迁移到自动生成的 settings.json，新增初始化、旧配置迁移、重启读取及配置损坏保护测试。

2026-09-07 反代修复：新增根路径跳转、HTTPS 外部 Origin、非标准端口二维码、反代改写 Host 后的配置地址校验、恶意转发头拒绝及 SSE 首帧测试。共 10 组测试通过，Web 构建和浏览器联动通过。用户已确认 UGOS 容器启动；实际 Lucky、电视异地播放仍待用户验收。

自动曲库与 TV 舞台更新：新增 NFO/海报读取、标签筛选、重复导入保留源文件、随机原唱与点播接管测试；真实 FFmpeg + 模拟远端 AI 验证完整入库双版本流程。stage-check 验证 720p 自动原唱播放、点歌切伴奏及整理预览。bili-sync 已核对源码格式，NAS 实际自动同步与真实 AI 音质仍待现场验证。
