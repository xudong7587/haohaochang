# v0.4.0 本地验证记录

2026-09-11，从 v0.3.14 `13631e4` 开发。测试使用临时数据库、合成媒体和 localhost 随机端口，不向正式曲库添加歌曲。

| 检查 | 结果与边界 |
| --- | --- |
| `npm test` | 156 项通过；真实 FFmpeg 生成 3840×2160 60fps，下载缓存及入库后保留尺寸和帧率；更新失败、修订竞争不发布半成品，成功换录音同时替换原唱和伴奏 |
| `npm run build` | 生产前端及旧 WebView 兼容包构建通过 |
| `tests/adaptive-player.browser.mjs` | 通过；缺海报卡片等高、下／上键在歌曲网格移动、侧栏收起／返回、焦点白底、全屏返回切换菜单、显式退出、热更新保留登录和队列 |
| `tests/native-player.browser.mjs` | 通过；模拟原生桥接，无 HTML 音视频解码副本、时钟插值上限、无周期 seek、旧会话丢弃、租约撤回、结束回调去重、画面边界和销毁。此项不代表 Android 解码实测 |
| `tests/library-ui.browser.mjs` | 通过；歌曲编辑新链接预览、整录音更新提交和修订号；原有资料草稿、歌词来源、候选核对、隐藏与恢复回归 |
| `tests/online-player.browser.mjs` | 通过；在线预览、裁剪、下载画质、歌词显示与微调、手机布局 |
| `scripts/player-check.mjs` | 通过；真实双音轨、暂停切换、错误后备、播放租约、提前结束的视频、旧资源延迟与自动播放恢复 |
| `tests/player-lease.browser.mjs` | 通过；网页接管、TV 优先、租约撤回后静音、旧结束请求拒绝、离线回退及延迟响应 |
| Python 整理协议 | 15 项通过，包含真实裁剪、视频保留、NVENC GPU 解码失败后保留硬件编码重试、再回退 CPU；未使用真实 AI 模型 |
| Python 并发与 PC 更新器 | 1 项并发测试、9 项更新器测试通过，包含版本化附件选择 |
| Android 构建与 lint | `assembleDebug lintDebug` 通过，0 个 lint 错误；APK versionName 0.4.0、versionCode 18，约 3.8 MB |
| `scripts/ui-check.mjs` | 通过；TV／扫码／手机队列／互动／后台草稿冲突与隐藏恢复 |
| Android Java 策略 | 9 项直接 JUnit 通过：连接／版本化附件更新规则与原生播放同源资源、前后台／租约／心跳超时、偏移精度和无效输入 |

本机中文路径下 Gradle `testDebugUnitTest` 启动测试类失败；Java 编译成功后使用同一测试类直接运行 JUnit，通过 9 项。CI 保留 Linux Gradle 单测。APK 构建与 lint 单独执行通过。

尚未验证真实电视的硬解码、音画同步、4K60/HDR 输出、原唱／伴奏切换和家庭网络。模拟器安装因 SDK 下载包无法解压而未完成。硬件编码自动选择与失败回退已验证逻辑，未分别在 NVIDIA、Intel、AMD 驱动上做性能验收。B站换源事务使用隔离下载／分离结果，未用 NAS 账号或真实模型发起新歌曲任务。

实机复测使用薛之谦《平庸》、杨宗纬《洋葱》、周深《花开忘忧》，对照周深《大鱼》、周杰伦《本草纲目》。安装新 APK 并更新 NAS 后，打开全屏“播放信息”记录解码器、分辨率、帧率、累计丢帧与缓存，再比较流畅度。需要兼容转换时手动处理对应歌曲，并核对转换后仍为源尺寸／帧率。
